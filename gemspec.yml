name: wsoc
summary:
  An example web server that tests the thoroughness and resilience of
  Web Spiders

description:
  The Web Spider Obstacle Course (WSOC) is an example web server that tests
  the thoroughness and resilience of Web Spiders.

license: MIT
authors: Postmodern
email: postmodern.mod3@gmail.com
homepage: http://github.com/postmodern/wsoc
has_yard: true

dependencies:
  json: ~> 1.4
  sinatra: ~> 1.0

development_dependencies:
  bundler: ~> 1.0
  yard: ~> 0.7
